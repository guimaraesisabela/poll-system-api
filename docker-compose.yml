services:
  postgres:
    image: postgres:16
    container_name: poll-system-db
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: poll_system
    ports:
      - "5432:5432"
    volumes:
      - poll_data:/var/lib/postgresql/data
volumes:
  poll_data:
